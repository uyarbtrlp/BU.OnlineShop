apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: catalogservice-ingress-srv
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/user-regex: 'true'
spec:
  rules:
    - host: catalogservice.com
      http:
        paths: 
          - path: /
            pathType: Prefix
            backend:
              service:
                name: catalogservice-clusterip-srv
                port:
                  number: 80