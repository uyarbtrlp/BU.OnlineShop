apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-srv
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/user-regex: 'true'
spec:
  rules:
    - host: basketservice.com
      http:
        paths: 
          - path: /
            pathType: Prefix
            backend:
              service:
                name: basketservice-clusterip-srv
                port:
                  number: 80