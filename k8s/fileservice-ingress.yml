apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fileservice-ingress-srv
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/user-regex: 'true'
spec:
  rules:
    - host: fileservice.com
      http:
        paths: 
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fileservice-clusterip-srv
                port:
                  number: 80